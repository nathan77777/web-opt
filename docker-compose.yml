services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: webopt_app
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_PORT: "5432"
      DB_NAME: webopt
      DB_USER: postgres
      DB_PASS: nathan
    ports:
      - "9000:80"
    command: sh -c "mkdir -p /var/www/html/uploads/articles && chown -R www-data:www-data /var/www/html/uploads && apache2-foreground"
    volumes:
      - ./:/var/www/html
      # Named volume so uploaded images persist across container restarts/rebuilds
      - uploads_data:/var/www/html/uploads

  db:
    image: postgres:15
    container_name: webopt_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: webopt
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: nathan
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./sql/01_init_tables.sql:/docker-entrypoint-initdb.d/01_init_tables.sql:ro

volumes:
  db_data:
  uploads_data: